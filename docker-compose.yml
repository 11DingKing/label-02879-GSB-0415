services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: sudoku-backend:latest
    container_name: sudoku-backend
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
